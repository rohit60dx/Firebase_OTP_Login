import 'package:flutter/material.dart';

class Sreach extends StatelessWidget {
  const Sreach({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("SERACH")),
    );
  }
}
